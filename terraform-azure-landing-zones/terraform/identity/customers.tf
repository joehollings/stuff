locals {
  customers = {
    # customer number
    # kab = {
    #   # actual customer 
    #   customer_name           = "kabeelah"
    #   rsv_soft_delete_enabled = false
    # }
  }
  # Don't change this
  root_ip_output = "kab"
  # change this to the new customer number BEFORE you deploy the domain controllers
  ip_output = ""
}